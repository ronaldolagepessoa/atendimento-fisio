# Setup & Infraestrutura — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Projeto Next.js 15 funcional com Prisma + PostgreSQL, NextAuth v5, Docker e configuração Railway — login funcionando, banco com schema completo, pronto para os próximos planos.

**Architecture:** Next.js App Router com TypeScript e Tailwind CSS. Prisma gerencia o banco PostgreSQL (Railway). NextAuth v5 (Auth.js) com Prisma Adapter para autenticação por email+senha com roles (admin/fisio). Dockerfile multi-stage para deploy no Railway.

**Tech Stack:** Next.js 15, TypeScript, Tailwind CSS 4, Prisma 6, PostgreSQL, NextAuth v5 (Auth.js), bcryptjs, Railway, Docker

---

## Estrutura de Arquivos

```
atendimento-fisio/
├── Dockerfile
├── .dockerignore
├── docker-compose.yml          # dev local com postgres
├── .env.example
├── .gitignore
├── next.config.ts
├── tailwind.config.ts
├── tsconfig.json
├── prisma/
│   └── schema.prisma           # schema completo de todos os módulos
├── src/
│   ├── auth.ts                 # NextAuth config (handlers, signIn, signOut, auth)
│   ├── middleware.ts            # proteção de rotas
│   ├── lib/
│   │   ├── prisma.ts           # singleton Prisma client
│   │   └── utils.ts            # cn() helper (clsx + tailwind-merge)
│   ├── types/
│   │   └── next-auth.d.ts      # extensão de tipos do NextAuth
│   └── app/
│       ├── layout.tsx
│       ├── globals.css
│       ├── api/
│       │   └── auth/
│       │       └── [...nextauth]/
│       │           └── route.ts
│       ├── (auth)/
│       │   └── login/
│       │       ├── page.tsx    # tela de login
│       │       └── actions.ts  # server action de login
│       └── (app)/
│           ├── layout.tsx      # layout com sidebar (protegido)
│           └── page.tsx        # redirect para /agenda
```

---

## Task 1: Scaffold Next.js e dependências

**Files:**
- Create: `package.json`, `next.config.ts`, `tsconfig.json`, `tailwind.config.ts`, `src/app/layout.tsx`, `src/app/globals.css`

- [ ] **Step 1: Inicializar o projeto Next.js**

```bash
cd c:/Users/ronal/Documents/Projetos/atendimento-fisio
npx create-next-app@latest . --typescript --tailwind --app --src-dir --import-alias "@/*" --no-git --yes
```

Responda `No` se perguntar sobre `turbopack` (Railway tem melhor suporte sem ele por ora).

- [ ] **Step 2: Instalar dependências**

```bash
npm install prisma @prisma/client next-auth@beta @auth/prisma-adapter bcryptjs
npm install -D @types/bcryptjs prisma
```

- [ ] **Step 3: Instalar utilitários de UI**

```bash
npm install clsx tailwind-merge lucide-react
```

- [ ] **Step 4: Verificar que o projeto inicia**

```bash
npm run dev
```

Esperado: servidor em `http://localhost:3000` sem erros no terminal.

- [ ] **Step 5: Commit inicial**

```bash
git add -A
git commit -m "feat: scaffold Next.js 15 com TypeScript e Tailwind"
```

---

## Task 2: Schema Prisma completo

**Files:**
- Create: `prisma/schema.prisma`

- [ ] **Step 1: Inicializar Prisma**

```bash
npx prisma init --datasource-provider postgresql
```

Isso cria `prisma/schema.prisma` e `.env` com `DATABASE_URL`.

- [ ] **Step 2: Escrever o schema completo**

Substitua o conteúdo de `prisma/schema.prisma`:

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ─── Auth ────────────────────────────────────────────────────────────────────

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

model User {
  id       String   @id @default(cuid())
  email    String   @unique
  name     String?
  password String?  // bcrypt hash
  role     Role     @default(FISIO)
  ativo    Boolean  @default(true)

  fisio    Fisioterapeuta? @relation(fields: [fisioId], references: [id])
  fisioId  String?

  accounts Account[]
  sessions Session[]

  createdAt DateTime @default(now())
}

enum Role {
  ADMIN
  FISIO
}

// ─── Domínio ─────────────────────────────────────────────────────────────────

model Fisioterapeuta {
  id    String  @id @default(cuid())
  nome  String
  email String  @unique
  cref  String?
  cor   String  @default("#6366f1") // hex, cor na agenda
  ativo Boolean @default(true)

  users            User[]
  pacienteFisios   PacienteFisio[]
  agendamentos     Agendamento[]
  frequencias      FrequenciaAgendamento[]
  tapings          Taping[]

  createdAt DateTime @default(now())
}

model Paciente {
  id              String    @id @default(cuid())
  nome            String
  telefone        String    // WhatsApp (ex: 5511999999999)
  cpf             String?   @unique
  dataNascimento  DateTime?
  observacoes     String?   @db.Text

  pacienteFisios PacienteFisio[]
  agendamentos   Agendamento[]
  frequencias    FrequenciaAgendamento[]
  pacotes        PacoteSessoes[]
  pagamentos     Pagamento[]
  tapings        Taping[]

  createdAt DateTime @default(now())
}

model PacienteFisio {
  id                String @id @default(cuid())
  corPersonalizada  String @default("#6366f1") // cor do paciente para este fisio

  paciente   Paciente       @relation(fields: [pacienteId], references: [id], onDelete: Cascade)
  pacienteId String
  fisio      Fisioterapeuta @relation(fields: [fisioId], references: [id], onDelete: Cascade)
  fisioId    String

  @@unique([pacienteId, fisioId])
}

model Procedimento {
  id        String  @id @default(cuid())
  nome      String
  valor     Decimal @db.Decimal(10, 2)
  descricao String? @db.Text
  ativo     Boolean @default(true)

  agendamentos Agendamento[]

  createdAt DateTime @default(now())
}

model PacoteSessoes {
  id             String       @id @default(cuid())
  totalSessoes   Int
  sessoesUsadas  Int          @default(0)
  dataInicio     DateTime
  validadeDias   Int          @default(40)
  dataExpiracao  DateTime     // dataInicio + validadeDias
  valorTotal     Decimal      @db.Decimal(10, 2)
  status         StatusPacote @default(ATIVO)

  paciente   Paciente @relation(fields: [pacienteId], references: [id], onDelete: Cascade)
  pacienteId String

  agendamentos Agendamento[]
  pagamentos   Pagamento[]

  createdAt DateTime @default(now())
}

enum StatusPacote {
  ATIVO
  EXPIRADO
  ESGOTADO
}

model FrequenciaAgendamento {
  id          String   @id @default(cuid())
  diasSemana  Int[]    // 0=dom, 1=seg, 2=ter, 3=qua, 4=qui, 5=sex, 6=sab
  horario     String   // "09:00"
  dataInicio  DateTime
  dataFim     DateTime
  descricao   String?

  paciente   Paciente       @relation(fields: [pacienteId], references: [id], onDelete: Cascade)
  pacienteId String
  fisio      Fisioterapeuta @relation(fields: [fisioId], references: [id], onDelete: Cascade)
  fisioId    String

  agendamentos Agendamento[]

  createdAt DateTime @default(now())
}

model Agendamento {
  id                  String            @id @default(cuid())
  dataHora            DateTime
  status              StatusAgendamento @default(AGENDADO)
  notificacaoEnviada  Boolean           @default(false)

  paciente      Paciente       @relation(fields: [pacienteId], references: [id], onDelete: Cascade)
  pacienteId    String
  fisio         Fisioterapeuta @relation(fields: [fisioId], references: [id], onDelete: Cascade)
  fisioId       String
  procedimento  Procedimento   @relation(fields: [procedimentoId], references: [id])
  procedimentoId String

  pacote      PacoteSessoes?        @relation(fields: [pacoteId], references: [id])
  pacoteId    String?
  frequencia  FrequenciaAgendamento? @relation(fields: [frequenciaId], references: [id])
  frequenciaId String?

  pagamento Pagamento?

  createdAt DateTime @default(now())
}

enum StatusAgendamento {
  AGENDADO
  REALIZADO
  CANCELADO
  FALTOU
}

model Pagamento {
  id             String       @id @default(cuid())
  valor          Decimal      @db.Decimal(10, 2)
  forma          FormaPagamento
  dataPagamento  DateTime     @default(now())
  observacao     String?      @db.Text

  paciente   Paciente @relation(fields: [pacienteId], references: [id], onDelete: Cascade)
  pacienteId String

  agendamento   Agendamento? @relation(fields: [agendamentoId], references: [id])
  agendamentoId String?      @unique

  pacote   PacoteSessoes? @relation(fields: [pacoteId], references: [id])
  pacoteId String?

  createdAt DateTime @default(now())
}

enum FormaPagamento {
  DINHEIRO
  PIX
  CARTAO
  TRANSFERENCIA
}

model Taping {
  id            String   @id @default(cuid())
  regiaCorporal String
  dataAplicacao DateTime
  dataRetirada  DateTime // dataAplicacao + 4 dias
  retirado      Boolean  @default(false)

  paciente   Paciente       @relation(fields: [pacienteId], references: [id], onDelete: Cascade)
  pacienteId String
  fisio      Fisioterapeuta @relation(fields: [fisioId], references: [id], onDelete: Cascade)
  fisioId    String

  createdAt DateTime @default(now())
}
```

- [ ] **Step 3: Confirmar relações bidirecionais**

Confirmar visualmente que `Paciente` contém `frequencias FrequenciaAgendamento[]` (já incluído no schema acima na linha após `agendamentos`).

- [ ] **Step 4: Commit do schema**

```bash
git add prisma/schema.prisma .env
git commit -m "feat: schema Prisma completo com todos os modelos"
```

---

## Task 3: Singleton Prisma Client e utilitários

**Files:**
- Create: `src/lib/prisma.ts`
- Create: `src/lib/utils.ts`

- [ ] **Step 1: Criar o singleton Prisma**

Crie `src/lib/prisma.ts`:

```typescript
import { PrismaClient } from "@prisma/client";

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined;
};

export const prisma =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: process.env.NODE_ENV === "development" ? ["error", "warn"] : ["error"],
  });

if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = prisma;
```

- [ ] **Step 2: Criar utilitário cn()**

Crie `src/lib/utils.ts`:

```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

- [ ] **Step 3: Commit**

```bash
git add src/lib/
git commit -m "feat: prisma singleton e cn() helper"
```

---

## Task 4: NextAuth v5 com Prisma Adapter

**Files:**
- Create: `src/auth.ts`
- Create: `src/app/api/auth/[...nextauth]/route.ts`
- Create: `src/types/next-auth.d.ts`

- [ ] **Step 1: Criar src/auth.ts**

```typescript
import NextAuth from "next-auth";
import Credentials from "next-auth/providers/credentials";
import { PrismaAdapter } from "@auth/prisma-adapter";
import { prisma } from "@/lib/prisma";
import bcrypt from "bcryptjs";

export const { handlers, signIn, signOut, auth } = NextAuth({
  adapter: PrismaAdapter(prisma),
  session: { strategy: "jwt" },
  pages: { signIn: "/login" },
  providers: [
    Credentials({
      credentials: {
        email: { label: "Email", type: "email" },
        password: { label: "Senha", type: "password" },
      },
      async authorize(credentials) {
        if (!credentials?.email || !credentials?.password) return null;

        const user = await prisma.user.findUnique({
          where: { email: credentials.email as string },
          include: { fisio: true },
        });

        if (!user || !user.password || !user.ativo) return null;

        const valid = await bcrypt.compare(
          credentials.password as string,
          user.password
        );
        if (!valid) return null;

        return {
          id: user.id,
          email: user.email,
          name: user.name,
          role: user.role,
          fisioId: user.fisioId ?? undefined,
        };
      },
    }),
  ],
  callbacks: {
    async jwt({ token, user }) {
      if (user) {
        token.role = (user as { role: string }).role;
        token.fisioId = (user as { fisioId?: string }).fisioId;
      }
      return token;
    },
    async session({ session, token }) {
      session.user.role = token.role as string;
      session.user.fisioId = token.fisioId as string | undefined;
      return session;
    },
  },
});
```

- [ ] **Step 2: Criar route handler**

Crie `src/app/api/auth/[...nextauth]/route.ts`:

```typescript
import { handlers } from "@/auth";

export const { GET, POST } = handlers;
```

- [ ] **Step 3: Extender tipos do NextAuth**

Crie `src/types/next-auth.d.ts`:

```typescript
import "next-auth";
import "next-auth/jwt";

declare module "next-auth" {
  interface User {
    role: string;
    fisioId?: string;
  }
  interface Session {
    user: {
      role: string;
      fisioId?: string;
    } & DefaultSession["user"];
  }
}

declare module "next-auth/jwt" {
  interface JWT {
    role?: string;
    fisioId?: string;
  }
}
```

- [ ] **Step 4: Adicionar AUTH_SECRET ao .env**

No arquivo `.env`, adicione (gere um valor seguro):

```bash
AUTH_SECRET="$(openssl rand -base64 32)"
```

Execute no terminal para gerar e copie o resultado:

```bash
openssl rand -base64 32
```

Cole o valor em `.env`:

```
AUTH_SECRET=<valor_gerado>
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/atendimento_fisio"
```

- [ ] **Step 5: Commit**

```bash
git add src/auth.ts src/app/api/ src/types/
git commit -m "feat: NextAuth v5 com Prisma adapter e JWT strategy"
```

---

## Task 5: Middleware de proteção de rotas

**Files:**
- Create: `src/middleware.ts`

- [ ] **Step 1: Criar middleware**

Crie `src/middleware.ts`:

```typescript
import { auth } from "@/auth";
import { NextResponse } from "next/server";

export default auth((req) => {
  const isLoggedIn = !!req.auth;
  const isAuthRoute = req.nextUrl.pathname.startsWith("/login");
  const isApiAuthRoute = req.nextUrl.pathname.startsWith("/api/auth");

  if (isApiAuthRoute) return NextResponse.next();

  if (!isLoggedIn && !isAuthRoute) {
    return NextResponse.redirect(new URL("/login", req.url));
  }

  if (isLoggedIn && isAuthRoute) {
    return NextResponse.redirect(new URL("/agenda", req.url));
  }

  return NextResponse.next();
});

export const config = {
  matcher: ["/((?!_next/static|_next/image|favicon.ico).*)"],
};
```

- [ ] **Step 2: Commit**

```bash
git add src/middleware.ts
git commit -m "feat: middleware de proteção de rotas via NextAuth"
```

---

## Task 6: Tela de Login

**Files:**
- Create: `src/app/(auth)/login/page.tsx`
- Create: `src/app/(auth)/login/actions.ts`

- [ ] **Step 1: Criar server action de login**

Crie `src/app/(auth)/login/actions.ts`:

```typescript
"use server";

import { signIn } from "@/auth";
import { AuthError } from "next-auth";
import { redirect } from "next/navigation";

export async function loginAction(formData: FormData) {
  const email = formData.get("email") as string;
  const password = formData.get("password") as string;

  try {
    await signIn("credentials", { email, password, redirectTo: "/agenda" });
  } catch (error) {
    if (error instanceof AuthError) {
      return { error: "Email ou senha incorretos." };
    }
    throw error;
  }
}
```

- [ ] **Step 2: Criar página de login**

Crie `src/app/(auth)/login/page.tsx`:

```typescript
"use client";

import { useState } from "react";
import { loginAction } from "./actions";

export default function LoginPage() {
  const [error, setError] = useState<string>();
  const [loading, setLoading] = useState(false);

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault();
    setLoading(true);
    setError(undefined);
    const formData = new FormData(e.currentTarget);
    const result = await loginAction(formData);
    if (result?.error) {
      setError(result.error);
      setLoading(false);
    }
  }

  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
      <div className="w-full max-w-sm bg-white rounded-xl shadow-sm p-8 space-y-6">
        <div className="text-center">
          <h1 className="text-2xl font-bold text-gray-900">Fisio Clínica</h1>
          <p className="text-sm text-gray-500 mt-1">Acesso ao sistema</p>
        </div>

        <form onSubmit={handleSubmit} className="space-y-4">
          <div>
            <label className="block text-sm font-medium text-gray-700 mb-1">
              Email
            </label>
            <input
              name="email"
              type="email"
              required
              className="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500"
            />
          </div>
          <div>
            <label className="block text-sm font-medium text-gray-700 mb-1">
              Senha
            </label>
            <input
              name="password"
              type="password"
              required
              className="w-full border border-gray-300 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500"
            />
          </div>

          {error && (
            <p className="text-sm text-red-600 bg-red-50 rounded-lg px-3 py-2">
              {error}
            </p>
          )}

          <button
            type="submit"
            disabled={loading}
            className="w-full bg-indigo-600 hover:bg-indigo-700 disabled:opacity-50 text-white font-medium rounded-lg py-2 text-sm transition-colors"
          >
            {loading ? "Entrando..." : "Entrar"}
          </button>
        </form>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add src/app/\(auth\)/
git commit -m "feat: tela de login com server action"
```

---

## Task 7: Layout raiz e estrutura de app protegido

**Files:**
- Modify: `src/app/layout.tsx`
- Create: `src/app/(app)/layout.tsx`
- Create: `src/app/(app)/page.tsx`

- [ ] **Step 1: Atualizar layout raiz**

Substitua `src/app/layout.tsx`:

```typescript
import type { Metadata } from "next";
import { Geist } from "next/font/google";
import "./globals.css";

const geist = Geist({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Fisio Clínica",
  description: "Sistema de gestão de clínica de fisioterapia",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="pt-BR">
      <body className={geist.className}>{children}</body>
    </html>
  );
}
```

- [ ] **Step 2: Criar layout do app protegido (placeholder para sidebar)**

Crie `src/app/(app)/layout.tsx`:

```typescript
import { auth } from "@/auth";
import { redirect } from "next/navigation";

export default async function AppLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const session = await auth();
  if (!session) redirect("/login");

  return (
    <div className="flex min-h-screen bg-gray-50">
      {/* Sidebar será implementada no Plano 2 */}
      <aside className="w-56 bg-slate-900 text-white flex flex-col p-4">
        <span className="font-bold text-lg mb-6">🏥 Fisio Clínica</span>
        <nav className="flex flex-col gap-1 text-sm text-slate-300">
          <a href="/agenda" className="hover:text-white px-3 py-2 rounded-lg hover:bg-slate-700">📅 Agenda</a>
          <a href="/pacientes" className="hover:text-white px-3 py-2 rounded-lg hover:bg-slate-700">👤 Pacientes</a>
          <a href="/fisios" className="hover:text-white px-3 py-2 rounded-lg hover:bg-slate-700">🧑‍⚕️ Fisioterapeutas</a>
          <a href="/procedimentos" className="hover:text-white px-3 py-2 rounded-lg hover:bg-slate-700">💊 Procedimentos</a>
          <a href="/pacotes" className="hover:text-white px-3 py-2 rounded-lg hover:bg-slate-700">📦 Pacotes</a>
          <a href="/pagamentos" className="hover:text-white px-3 py-2 rounded-lg hover:bg-slate-700">💰 Pagamentos</a>
          <a href="/taping" className="hover:text-white px-3 py-2 rounded-lg hover:bg-slate-700">🩹 Taping</a>
          <a href="/relatorios" className="hover:text-white px-3 py-2 rounded-lg hover:bg-slate-700">📊 Relatórios</a>
        </nav>
      </aside>
      <main className="flex-1 p-6">{children}</main>
    </div>
  );
}
```

- [ ] **Step 3: Criar página raiz com redirect**

Crie `src/app/(app)/page.tsx`:

```typescript
import { redirect } from "next/navigation";

export default function HomePage() {
  redirect("/agenda");
}
```

- [ ] **Step 4: Commit**

```bash
git add src/app/
git commit -m "feat: layout raiz e estrutura de app protegido com sidebar placeholder"
```

---

## Task 8: Docker e configuração Railway

**Files:**
- Create: `Dockerfile`
- Create: `.dockerignore`
- Create: `docker-compose.yml`
- Create: `.env.example`
- Modify: `.gitignore`

- [ ] **Step 1: Criar Dockerfile multi-stage**

Crie `Dockerfile`:

```dockerfile
FROM node:22-alpine AS base

FROM base AS deps
WORKDIR /app
COPY package*.json ./
RUN npm ci

FROM base AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN npx prisma generate
RUN npm run build

FROM base AS runner
WORKDIR /app
ENV NODE_ENV=production

RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs

COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static
COPY --from=builder /app/prisma ./prisma
COPY --from=builder /app/node_modules/.prisma ./node_modules/.prisma
COPY --from=builder /app/node_modules/@prisma ./node_modules/@prisma

USER nextjs
EXPOSE 3000
ENV PORT=3000
ENV HOSTNAME="0.0.0.0"

CMD ["sh", "-c", "npx prisma migrate deploy && node server.js"]
```

- [ ] **Step 2: Habilitar output standalone no Next.js**

Abra `next.config.ts` e adicione `output: "standalone"`:

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "standalone",
};

export default nextConfig;
```

- [ ] **Step 3: Criar .dockerignore**

Crie `.dockerignore`:

```
node_modules
.next
.env
.env.local
*.md
.git
.gitignore
docker-compose.yml
```

- [ ] **Step 4: Criar docker-compose.yml para desenvolvimento local**

Crie `docker-compose.yml`:

```yaml
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: atendimento_fisio
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
```

- [ ] **Step 5: Criar .env.example**

Crie `.env.example`:

```
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/atendimento_fisio"
AUTH_SECRET="gere-com-openssl-rand-base64-32"
ZAPI_INSTANCE_ID=""
ZAPI_TOKEN=""
ZAPI_CLIENT_TOKEN=""
```

- [ ] **Step 6: Atualizar .gitignore**

Certifique-se que `.env` (não `.env.example`) está no `.gitignore`. O `create-next-app` já adiciona, mas verifique:

```bash
grep "^\.env$" .gitignore || echo ".env" >> .gitignore
```

- [ ] **Step 7: Commit**

```bash
git add Dockerfile .dockerignore docker-compose.yml .env.example next.config.ts .gitignore
git commit -m "feat: Docker multi-stage e docker-compose para dev local"
```

---

## Task 9: Migrations e seed do primeiro usuário admin

**Files:**
- Create: `prisma/seed.ts`
- Modify: `package.json`

- [ ] **Step 1: Subir PostgreSQL local**

```bash
docker compose up -d
```

Esperado: container `postgres` rodando em `localhost:5432`.

- [ ] **Step 2: Rodar migration inicial**

```bash
npx prisma migrate dev --name init
```

Esperado: migrations aplicadas, Prisma Client gerado.

- [ ] **Step 3: Criar seed com usuário admin**

Crie `prisma/seed.ts`:

```typescript
import { PrismaClient, Role } from "@prisma/client";
import bcrypt from "bcryptjs";

const prisma = new PrismaClient();

async function main() {
  const hash = await bcrypt.hash("admin123", 12);

  await prisma.user.upsert({
    where: { email: "admin@clinica.com" },
    update: {},
    create: {
      email: "admin@clinica.com",
      name: "Admin",
      password: hash,
      role: Role.ADMIN,
    },
  });

  console.log("✅ Seed concluído — admin@clinica.com / admin123");
}

main()
  .catch(console.error)
  .finally(() => prisma.$disconnect());
```

- [ ] **Step 4: Adicionar script de seed no package.json**

No `package.json`, adicione dentro de `"scripts"`:

```json
"db:seed": "tsx prisma/seed.ts"
```

E adicione no nível raiz do package.json:

```json
"prisma": {
  "seed": "tsx prisma/seed.ts"
}
```

Instale o executor:

```bash
npm install -D tsx
```

- [ ] **Step 5: Rodar seed**

```bash
npm run db:seed
```

Esperado: `✅ Seed concluído — admin@clinica.com / admin123`

- [ ] **Step 6: Testar login**

```bash
npm run dev
```

Acesse `http://localhost:3000` → deve redirecionar para `/login` → faça login com `admin@clinica.com` / `admin123` → deve redirecionar para `/agenda` (página em branco por ora, sem erro 500).

- [ ] **Step 7: Commit final**

```bash
git add prisma/ package.json
git commit -m "feat: migration inicial e seed com usuário admin"
```

---

## Task 10: Verificar build Docker local

- [ ] **Step 1: Build da imagem Docker**

```bash
docker build -t atendimento-fisio .
```

Esperado: build completo sem erros, imagem `atendimento-fisio` criada.

- [ ] **Step 2: Testar imagem com banco local**

```bash
docker run --rm \
  -e DATABASE_URL="postgresql://postgres:postgres@host.docker.internal:5432/atendimento_fisio" \
  -e AUTH_SECRET="qualquer-string-de-teste" \
  -p 3001:3000 \
  atendimento-fisio
```

Esperado: app rodando em `http://localhost:3001`, login funcionando.

- [ ] **Step 3: Commit de fechamento**

```bash
git add -A
git commit -m "chore: verificação de build Docker confirmada"
```

---

## Checklist de Entrega

Ao final do Plano 1, o repositório deve ter:

- [ ] `npm run dev` funciona, login acessível em `/login`
- [ ] Login com `admin@clinica.com` / `admin123` redireciona para `/agenda`
- [ ] Rota sem login redireciona para `/login`
- [ ] `docker build` completa sem erros
- [ ] `npx prisma migrate dev` aplica todas as tabelas
- [ ] `.env.example` sem segredos reais commitado
- [ ] `.env` no `.gitignore`
